export const startTimer = () => ({
    type: 'START_TIMER',
  });
  
  export const stopTimer = () => ({
    type: 'STOP_TIMER',
  });
  
  export const incrementTimer = () => ({
    type: 'INCREMENT_TIMER',
  });